import { noneStandaloneApp } from './none-standalone-app';

describe('noneStandaloneApp', () => {
  it('should work', () => {
    expect(noneStandaloneApp()).toEqual('none-standalone-app');
  });
});
